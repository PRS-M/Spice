@model Spice.Models.ViewModels.IndexViewModel

<br />

@if (Model.Coupon.Count() > 0)
{
    <div class="border">
        <div class="carousel" data-ride="carousel" data-interval="2500">
            @for (int i = 0; i < Model.Coupon.Count(); i++)
            {
                if (i == 0)
                {
                    var base64 = Convert.ToBase64String(Model.Coupon.ToList()[i].Picture);
                    var imgSource = String.Format("data:image/jpg; base64,{0}", base64);

                    <div class="carousel-item active">
                        <img src="@imgSource" height="50px" class="d-block w-100">
                    </div>
                }
                else
                {
                    var base64 = Convert.ToBase64String(Model.Coupon.ToList()[i].Picture);
                    var imgSource = String.Format("data:image/jpg; base64,{0}", base64);

                    <div class="carousel-item">
                        <img src="@imgSource" height="50px" class="d-block w-100">
                    </div>
                }
            }
        </div>
    </div>
}

<br />

<div class="backgroundWhite container">
    @foreach(var category in Model.Category)
    {
        <div class="row" id="menu-wrapper">
            <partial name="_ThumbnailAreaPartial" model="Model.MenuItem.Where(mi => mi.Category.Name.Equals(category.Name))" />
        </div>
    }
</div>